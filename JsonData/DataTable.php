<?php

$count = 3;

   for ($i=0 ; $i<$count ; $i++)
   {
       $c = 0;//กรณีส่งเข้าตาราง ไม่จำเป็นต้องมีชื่อ key object เพราะไม่ได้ใช้ชื่อในการเรียกใช้ ใช้เป็น index ได้เลย
       $tr[] = '';
       $month;
       if($i==0){$month="ตุลาคม";}else if($i==1){$month="พฤศจิกายน";}else{$month="ธันวาคม";}
       tr.accumulate(c+"",month);c++;
       tr.accumulate(c+"",14+i);c++;
       tr.accumulate(c+"",16+i);c++;
       tr.accumulate(c+"",12+i);c++;
       tr.accumulate(c+"",19+i);c++;
       tr.accumulate(c+"",17+i);c++;
       tr.accumulate(c+"",18+i);
       //tr.keys();
       table.put(tr);
   }
